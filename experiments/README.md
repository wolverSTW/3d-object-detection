# Experiments

This directory stores experiment outputs, checkpoints, logs, and metrics for the thesis project.

Suggested structure:

```text
experiments/
├── baseline/
├── geometry/
├── ablation/
├── distance/
└── README.md
```

Each experiment directory can hold configuration files, logs, checkpoints, and result tables.
